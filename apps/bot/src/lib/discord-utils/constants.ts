/**
 * Discord.js Message Flags
 * Using numeric values to avoid TypeScript issues with MessageFlags enum
 */

/**
 * Ephemeral flag - makes messages only visible to the command user
 * Equivalent to MessageFlags.Ephemeral
 */
export const EPHEMERAL_FLAG = 64;

/**
 * Other common flags for reference:
 * - SUPPRESS_EMBEDS = 4
 * - SUPPRESS_NOTIFICATIONS = 4096
 * - IS_VOICE_MESSAGE = 8192
 */
